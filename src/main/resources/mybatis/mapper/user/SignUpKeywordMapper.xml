<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vvs.platform.dao.user.SignUpKeywordDAO">

	<!-- 전체 키워드 목록 조회(회원가입시 선택용) -->
	<select id="getAllKeywords" resultType="KeywordDTO">
		select
			null as userId, 		-- 전체 목록이므로 userId는 null
			k.SEQ as keywordId, 	-- keyword테이블의 SEQ
			k.KEYWORD_CONTENT as keyword --keyword테이블의 KEYWORD_CONTENT
		from KEYWORD k
		order by k.SEQ                  
	</select>
	
	<!-- 선택된 키워드 저장 -->
	<insert id="insertUserKeyword" parameterType="KeywordDTO">
		insert into USER_KEYWORDS (
			USER_ID,
			KEYWORD_ID
		) values (
			#{userId},
			#{keywordId)
		)
	</insert>
	
</mapper>
